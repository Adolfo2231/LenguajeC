#include <stdio.h>

int main(){
	//Ciclo for(Ini.contador,condicion a evaluar, incrementar contador)
	//Aunque este el var++ la primera vez no pasa
	for(int contador = 1;contador <= 5; contador++){
		printf("\nBuenos dias");
		printf("\nVariable contador: %d\n", contador);
		
	}	
	return 0;

}


